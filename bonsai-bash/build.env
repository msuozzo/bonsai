# Grammar pins for bonsai-bash, consumed verbatim as `docker build`
# --build-arg values by build/regen.sh.
GRAMMAR_NAME=bash
GRAMMAR_REPO=https://github.com/tree-sitter/tree-sitter-bash.git
GRAMMAR_TAG=v0.25.1
GRAMMAR_DIR=tree-sitter-bash
LANGUAGE_SYMBOL=tree_sitter_bash
GRAMMAR_HAS_SCANNER=1
