# Grammar pins for bonsai-dockerfile, consumed verbatim as `docker
# build` --build-arg values by build/regen.sh.
GRAMMAR_NAME=dockerfile
GRAMMAR_REPO=https://github.com/camdencheek/tree-sitter-dockerfile.git
GRAMMAR_TAG=v0.2.0
GRAMMAR_DIR=tree-sitter-dockerfile
LANGUAGE_SYMBOL=tree_sitter_dockerfile
GRAMMAR_HAS_SCANNER=1
